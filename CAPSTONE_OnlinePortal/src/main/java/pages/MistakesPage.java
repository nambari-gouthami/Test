package pages;

import java.time.Duration;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.FindBy;
import org.openqa.selenium.support.PageFactory;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

/*public class MistakesPage {
	WebDriver driver;
	By mistakesHeader=By.xpath("//h1[contains(text(),'Mistakes')]");
	By mistakesList=By.xpath("//div[@class,'mistake-card')]");
	By firstMistake=By.xpath("/(/div[contains(@class,'mistake-card')])[1]");
	By mistakeDetails=By.xpath("//div[contains(@class,'question-detail')]");
	
	
	public void MistakePage(WebDriver driver) {
		this.driver=driver;
	}
	public boolean isMistakesPageDisplayed() {
		return
				driver.findElement(mistakesHeader).isDisplayed();
	}
	public boolean isMistakesListDisplayed() {
		return driver.findElement(mistakesList).isDisplayed();
	}
	public void clickFirstMistake() {
		driver.findElement(firstMistake).click();
	}
	public boolean isMistakeDetailsDisplayed() {
		return
				driver.findElement(mistakeDetails).isDisplayed();
	}
	
	

}*/
